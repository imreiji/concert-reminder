# dekimasen.app - .app is HSTS-preloaded (browsers force HTTPS), Caddy's
# automatic Let's Encrypt certificates satisfy that with zero config.
dekimasen.app {
    reverse_proxy 127.0.0.1:8000
}

# www redirects to the apex
www.dekimasen.app {
    redir https://dekimasen.app{uri} permanent
}
