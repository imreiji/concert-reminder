# Demo Parity Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close WISHLIST entry #8 — three app changes the demo was right about, and nine demo frames recording surfaces the app already ships.

**Architecture:** Two independent branches split by review surface. Branch A (Tasks 1-3) touches app templates/CSS and carries tests. Branch B (Tasks 4-8) touches only the concept demos in `docs/superpowers/demo/` — no app code, no tests, because they are documentation. Task 9 is bookkeeping. Branch A and Branch B share no files and may ship in either order.

**Tech Stack:** Jinja2 templates, plain CSS (no preprocessor), pytest, Babel `.po` catalogues.

## Global Constraints

- **Tests and lint must pass before any commit:** `uv run --isolated pytest -q` and `uv run --isolated ruff check .`
- **Always `uv run --isolated`** — an external process holds a lock on `.venv`; never `uv sync`.
- **Sentence case everywhere** ("Add group", not "Add Group").
- **Radiuses: 3px default, 999px chips, 4px overlay cards, 50% circles, bottom sheets `14px 14px 0 0`.** Never 6px or 8px — there is a sweep test.
- **Editing existing English copy must either keep the msgid byte-identical or update BOTH `.po` catalogues** (`src/app/translations/ja/LC_MESSAGES/messages.po` and `.../zh/...`). `tests/test_i18n_catalogues.py` fails on anything untranslated.
- **Never interpolate user-controlled text into inline `on*` handlers** (invariant 7). Use `data-` attributes read via `dataset`.
- **Demos are high fidelity**, not wireframes: real CSS on the real design tokens, real Japanese content. Match the surrounding file's style.
- **Tasks 4-7 all modify `docs/superpowers/demo/dekimasen-demo.html`.** Run them **sequentially**, never in parallel — concurrent edits to one file will conflict.
- Commit messages end with:
  ```
  Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
  ```

---

## BRANCH A — app changes

### Task 1: "Auto-apply" becomes "Auto-apply preset"

**Files:**
- Modify: `src/app/web/templates/preferences.html:58`
- Modify: `src/app/translations/ja/LC_MESSAGES/messages.po:2761-2762`
- Modify: `src/app/translations/zh/LC_MESSAGES/messages.po:2759-2760`
- Test: none new — see Step 3

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces: nothing later tasks rely on. Fully independent.

**Why:** The toggle carries its meaning only in a `title` attribute, which is invisible on touch. The demo has said "Auto-apply preset" since it was written.

- [ ] **Step 1: Change the label**

In `src/app/web/templates/preferences.html`, line 58 currently reads:

```html
                  title="{{ _('Add my default preset to new matching events') }}">{{ _("Auto-apply") }}</button>
```

Change **only** the visible label — leave the `title` exactly as it is:

```html
                  title="{{ _('Add my default preset to new matching events') }}">{{ _("Auto-apply preset") }}</button>
```

Do **not** touch line 31 or line 41. Line 31 is prose inside a `{% trans %}` block that reads "Auto-apply adds your default preset" — already correct English, and changing it would re-key a much larger msgid. Line 41 is a code comment.

- [ ] **Step 2: Re-key both catalogues**

In `src/app/translations/ja/LC_MESSAGES/messages.po`, find:

```po
msgid "Auto-apply"
msgstr "自動適用"
```

Replace with:

```po
msgid "Auto-apply preset"
msgstr "プリセットを自動適用"
```

In `src/app/translations/zh/LC_MESSAGES/messages.po`, find:

```po
msgid "Auto-apply"
msgstr "自动应用"
```

Replace with:

```po
msgid "Auto-apply preset"
msgstr "自动应用预设"
```

Leave the `#:` source-reference comment lines above each entry alone; they are regenerated by `pybabel` and are not load-bearing here.

- [ ] **Step 3: Run the catalogue test to verify it passes**

Run: `uv run --isolated pytest tests/test_i18n_catalogues.py -q`
Expected: PASS.

This is the gate for this task and no new test is needed: the catalogue test fails on any untranslated msgid, so a changed English string that did not reach both `.po` files is caught by construction. **Verify this claim rather than trusting it** — temporarily revert only the ja `.po` edit, re-run, and confirm it FAILS. Then restore the edit. If it does not fail, stop and report: the assumption behind "no new test" is wrong and this task needs one.

- [ ] **Step 4: Measure the phone layout, before and after**

This is a required deliverable, not optional. The label grows by one word inside a `.swb` that sits in `.subrow`'s `.sw` span beside `Notify` and `Unfollow`. That row is already tight on a phone.

Start a dev server against a throwaway DB and put a real browser at the real widths — do not reason about it:

```bash
export SCRATCH="$(mktemp -d)"
export DATABASE_URL="sqlite+aiosqlite:///$SCRATCH/harness.db"
export SESSION_SECRET="0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
export DISCORD_TOKEN=""
export BASE_URL="http://127.0.0.1:8099"
export WEB_PORT=8099
uv run --isolated alembic upgrade head
uv run --isolated python -m app.main
```

You need a signed-in user with at least one followed tag for `.subrow` to render. Seed one directly, then measure `/preferences` at **365px and 320px**, in **both English and Japanese** (the 🌐 cycle chip switches language), and record for each: the `.subrow` height and whether `.sw` wrapped to a second line.

Report the eight numbers (2 widths x 2 languages x before/after). **If the row wraps where it did not before, that is a finding to report — not a reason to abandon the change, and not something to silently fix.** Stop and report it.

- [ ] **Step 5: Run the full suite and lint**

Run: `uv run --isolated pytest -q` then `uv run --isolated ruff check .`
Expected: all pass, no lint findings.

- [ ] **Step 6: Commit**

```bash
git add src/app/web/templates/preferences.html src/app/translations/ja/LC_MESSAGES/messages.po src/app/translations/zh/LC_MESSAGES/messages.po
git commit -m "$(cat <<'EOF'
fix(prefs): the Auto-apply toggle says what it applies

It carried its meaning in a title attribute, which is invisible on touch.
The demo has said "Auto-apply preset" since it was written.

A msgid change, so both catalogues are re-keyed with it.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

### Task 2: Hoist the new-tag dialog footer out of its form

**Files:**
- Modify: `src/app/web/templates/tags.html` (the `#new-tag-dialog` block, roughly lines 361-463)
- Test: `tests/test_tags.py`

**Interfaces:**
- Consumes: nothing from earlier tasks.
- Produces: nothing later tasks rely on.

**Why:** The `.df` footer is the last child of `<form class="db">`, which is `display: grid; padding: 1rem; gap: .9rem`, so its `border-top` stops short of the dialog edges and a grid gap floats it off the fields. The demo has `.df` as a sibling of the body, spanning the dialog. The app diverged because **its footer holds a real `type="submit"`** and so had to live inside the form; the demo's buttons are `type="button"` fakes. The fix is `form="<id>"` on the submit button.

**This is the only change in the plan that can break a working feature.** Write the test first.

- [ ] **Step 1: Write the failing test**

Add to `tests/test_tags.py`. Match the file's existing style — it already has a `login_as` helper and `EDITOR_ID`:

```python
def test_new_tag_footer_submit_still_submits(client):
    """The create button was hoisted OUT of its <form> so the dialog footer can
    span the dialog (demo parity), and only still works because of the
    form="new-tag-form" attribute.

    Mutation this must fail against: dropping that attribute. The button then
    renders identically, the page renders identically, and pressing it does
    NOTHING -- which a "does the page render" test sails straight past.
    """
    login_as(client, EDITOR_ID, "reiji")
    page = client.get("/tags")
    assert 'id="new-tag-form"' in page.text, "the form must carry the id"
    assert 'form="new-tag-form"' in page.text, (
        "the submit button lives outside the form and needs this to submit"
    )
    r = client.post("/tags", data={
        "name_en": "Hoisted", "name_zh": "Hoisted", "name": "Hoisted", "kind": "artist",
    })
    assert r.status_code in (200, 303)
```

- [ ] **Step 2: Run it to make sure it fails**

Run: `uv run --isolated pytest tests/test_tags.py::test_new_tag_footer_submit_still_submits -q`
Expected: FAIL on the first assertion — `id="new-tag-form"` is not in the page yet.

- [ ] **Step 3: Give the form an id**

In `src/app/web/templates/tags.html`, line 366 currently reads:

```html
  <form method="post" action="/tags" class="db" data-variant-scope data-variant-guard>
```

Change to:

```html
  <form method="post" action="/tags" class="db" id="new-tag-form" data-variant-scope data-variant-guard>
```

- [ ] **Step 4: Move the footer out and point the button at the form**

The block at the end of that dialog currently reads:

```html
    <div class="df">
      <button class="btn">{{ _("Create tag") }}</button>
      <button class="btn quiet" type="button" onclick="document.getElementById('new-tag-dialog').close()">{{ _("Cancel") }}</button>
    </div>
  </form>
</dialog>
```

Change to — note the `</form>` now closes **before** the footer, and the submit gains `form=`:

```html
  </form>
  {#- The footer is a SIBLING of the form, not its last child, so its border-top
      spans the dialog instead of stopping inside the form's 1rem padding (demo
      parity). The submit therefore sits outside the form it submits, which
      works only because of form="new-tag-form" -- drop that attribute and this
      button silently becomes inert. -#}
  <div class="df">
    <button class="btn" form="new-tag-form">{{ _("Create tag") }}</button>
    <button class="btn quiet" type="button" onclick="document.getElementById('new-tag-dialog').close()">{{ _("Cancel") }}</button>
  </div>
</dialog>
```

- [ ] **Step 5: Run the test to verify it passes**

Run: `uv run --isolated pytest tests/test_tags.py::test_new_tag_footer_submit_still_submits -q`
Expected: PASS.

- [ ] **Step 6: Verify the test catches its named mutation**

Temporarily delete ` form="new-tag-form"` from the button, re-run the test, and confirm it FAILS. Restore the attribute. If it passes without the attribute, the test is a proxy assertion — stop and report.

- [ ] **Step 7: Check the tag EDIT dialog for the same shape**

`src/app/web/templates/tags.html:178` has a `.df` footer too. Read it.

- If its footer also holds a real submit nested inside a form, **apply the same change to it** (its own form id, e.g. `edit-tag-form-{{ t.id }}` since it renders per-tag in a loop — make sure the id is unique per dialog, and that the button's `form=` matches its own dialog's form, not another tag's).
- If it does not, leave it and **say so explicitly in the commit message**.

What must not happen is it being left behind by accident.

- [ ] **Step 8: Confirm the dialog visually**

Open `/tags` on a dev server, press "+ New tag", and confirm: the footer's top border now runs the full width of the dialog, and pressing "Create tag" still creates a tag. A screenshot in the task report is welcome.

- [ ] **Step 9: Run the full suite and lint**

Run: `uv run --isolated pytest -q` then `uv run --isolated ruff check .`
Expected: all pass.

- [ ] **Step 10: Commit**

```bash
git add src/app/web/templates/tags.html tests/test_tags.py
git commit -m "$(cat <<'EOF'
fix(tags): the new-tag dialog footer spans the dialog again

Its border-top stopped inside the form's 1rem padding because the footer
was the form's last child -- which it had to be, since it holds a real
submit and the demo's equivalent buttons are type="button" fakes.

Hoisted out with form="new-tag-form" on the submit. The test is written
against exactly the mutation that makes this dangerous: drop the attribute
and the button renders identically and does nothing.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

### Task 3: `text-wrap: balance` on `.lede h1`

**Files:**
- Modify: `src/app/web/static/style.css:1512`
- Test: `tests/test_theme_and_tokens.py`

**Interfaces:**
- Consumes: nothing.
- Produces: nothing.

**Why:** `.lede h1` is the heading of the 403/404/422/500 error pages. It is the only 1.6rem+ display heading in the file without balanced wrapping.

**OWNER RULING (2026-08-11): fix `.legal h1` too.** It is 1.7rem — the same size as `.chead h1`, which already balances — and lacking it was the same oversight. So the rule the pinned set expresses is coherent: **every display heading 1.6rem and up balances its wrap**, and the 1.4rem generic `h1` deliberately does not. No longer blocked.

- [ ] **Step 1: Write the failing test**

Add to `tests/test_theme_and_tokens.py`, next to the other sweeps.

```python
BALANCED = (".hero .promise", ".ctafoot h2", ".chead h1", ".lede h1", ".legal h1")


def test_display_headings_balance_their_wrap():
    """Every display heading 1.6rem and up balances its line breaks; the
    generic `h1` (1.4rem, a fallback) deliberately does not.

    Deliberately a PINNED SET, not a sweep over every h1 rule: only one of the
    three selectors that already carried this is an h1, so "every h1 balances"
    is not the real pattern and a sweep would fail on the 1.4rem fallback.

    Two were missing it (owner ruling 2026-08-11 to fix both): `.lede h1`, the
    403/404/422/500 heading, and `.legal h1`, which is the same 1.7rem as
    `.chead h1` beside it.

    Mutation this must fail against: removing `text-wrap: balance` from any one
    selector in the set, including the three that already had it.
    """
    missing = [sel for sel in BALANCED if _decls(sel).get("text-wrap") != "balance"]
    assert not missing, f"display headings not balancing their wrap: {missing}"
```

Note `_decls(selector)` already exists in this file and returns the declarations of the rule whose selector is exactly `selector`; it asserts the rule exists, so a renamed selector fails loudly rather than silently passing.

**It returns a dict keyed by bare property name** (`{"text-wrap": "balance"}`), so the check must be `.get("text-wrap") != "balance"`. An earlier draft of this plan wrote `"text-wrap: balance" not in _decls(sel)`, which tests dict-KEY membership against a `prop: value` string and is therefore unconditionally true — the test could never pass, even after the fix. Corrected here after the Task 3 implementer caught it.

- [ ] **Step 2: Run it to make sure it fails**

Run: `uv run --isolated pytest tests/test_theme_and_tokens.py::test_display_headings_balance_their_wrap -q`
Expected: FAIL listing `['.lede h1', '.legal h1']`.

- [ ] **Step 3: Add the declaration**

`src/app/web/static/style.css` line 1512 currently reads:

```css
.lede h1 { margin: 0 0 .4rem; font-size: 1.6rem; letter-spacing: -.02em; font-weight: 700; }
```

Change to:

```css
.lede h1 { margin: 0 0 .4rem; font-size: 1.6rem; letter-spacing: -.02em; font-weight: 700; text-wrap: balance; }
```

And line 98 gets the same treatment:

```css
.legal h1 { font-size: 1.7rem; letter-spacing: -.025em; font-weight: 700; margin: 0 0 .3rem; text-wrap: balance; }
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `uv run --isolated pytest tests/test_theme_and_tokens.py::test_display_headings_balance_their_wrap -q`
Expected: PASS.

- [ ] **Step 5: Verify the test catches its named mutation**

Temporarily remove `text-wrap: balance` from `.chead h1` (one of the selectors that already had it), re-run, confirm FAIL, restore. This proves the test guards the whole set and not just the line you added.

- [ ] **Step 6: Run the full suite and lint**

Run: `uv run --isolated pytest -q` then `uv run --isolated ruff check .`

- [ ] **Step 7: Commit**

```bash
git add src/app/web/static/style.css tests/test_theme_and_tokens.py
git commit -m "$(cat <<'EOF'
fix(css): the error-page heading balances its wrap like every other

.lede h1 is the 403/404/422/500 heading and was the one 1.6rem+ display
heading in the file without text-wrap: balance.

The test pins a SET rather than sweeping every h1: only one of the three
selectors that already carried this is an h1, so "every h1 balances" is
not the pattern and a sweep would fail on the 1.4rem fallback.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

## BRANCH B — demo frames

No tests. These are documentation files. **Tasks 4-7 all edit `dekimasen-demo.html` — run them one at a time.**

Before starting any Branch B task, open the target demo in a browser and read the surrounding frames. Match their density, their CSS placement (rules live in the single `<style>` block near the top, grouped by surface with a `/* ---- surface name ---- */` banner comment), and their content style (real Japanese names, plausible dates).

**How to check dark mode in a demo.** The demos have **no theme toggle control** (verified). They define `:root[data-theme="dark"]`, `:root[data-theme="light"]` and an `@media (prefers-color-scheme: dark)` block. To see the dark palette, set the attribute in devtools:

```js
document.documentElement.setAttribute("data-theme", "dark")   // and "light" to go back
```

Every "confirm in both themes" step below means exactly this. Do not skip it — the app's own convention is that new components are styled against both directions, not just light.

### Task 4: The two reversals — member list and pick tiles

**Files:**
- Modify: `docs/superpowers/demo/dekimasen-demo.html` (member list ~line 2082; pick tiles ~line 1134)

**Interfaces:**
- Consumes: nothing.
- Produces: nothing.

**Why:** These are the two places the merit review found the **demo** wrong. Both need a comment explaining why, or the next reader "fixes" them back.

- [ ] **Step 1: Replace the truncated member list**

At `docs/superpowers/demo/dekimasen-demo.html` around line 2082, the tag edit dialog currently has four member chips then:

```html
        <span class="chip">+5 more</span>
```

Replace that single line with the remaining five members rendered as real removable chips, matching the four above them exactly:

```html
        <span class="chip">斉藤朱夏 <button class="x" type="button" style="background:none;border:0;cursor:pointer;color:var(--dim)">&times;</button></span><span class="chip">鈴木愛奈 <button class="x" type="button" style="background:none;border:0;cursor:pointer;color:var(--dim)">&times;</button></span><span class="chip">降幡愛 <button class="x" type="button" style="background:none;border:0;cursor:pointer;color:var(--dim)">&times;</button></span><span class="chip">高槻かなこ <button class="x" type="button" style="background:none;border:0;cursor:pointer;color:var(--dim)">&times;</button></span><span class="chip">法元明菜 <button class="x" type="button" style="background:none;border:0;cursor:pointer;color:var(--dim)">&times;</button></span>
```

Immediately above the `<div class="memb" ...>` that contains them, add:

```html
      <!-- No "+N more" truncation, deliberately (2026-08-11). This is the EDIT
           dialog: every chip carries a delete x and you opened it to change
           membership, so an inert "+N more" with no way to expand makes those
           members unreachable on the one surface built for reaching them.
           Truncation is right for a DISPLAY of members; this is not one. -->
```

- [ ] **Step 2: Replace the pick tiles**

At around line 1134, each tile currently reads:

```html
      <button class="tile" type="button" aria-pressed="true">
```

Change **every** tile in that `#pickList` block to a label wrapping a visually-hidden checkbox, keeping each tile's inner `<span>`s exactly as they are. The first one becomes:

```html
      <label class="tile">
        <input type="checkbox" class="visually-hidden" checked>
```

and its closing `</button>` becomes `</label>`. Repeat for every tile in the block — read the file and convert them all; leaving a mix would be worse than either shape.

Add above the `<div class="pick" id="pickList">`:

```html
    <!-- A label wrapping a visually-hidden checkbox, NOT <button aria-pressed>
         (changed 2026-08-11). They render identically; the difference is that a
         real form control submits with JavaScript disabled and a button cannot
         -- aria-pressed needs a script to track state and a hidden input to
         carry it, and this app keeps its forms working without JS. Screen
         readers announce "checked" rather than "pressed"; both are fine.
         Do not "fix" this back to a button. -->
```

- [ ] **Step 3: Add the visually-hidden rule if the demo lacks it**

The demo does **not** have `.visually-hidden` (verified — zero occurrences). Add it next to the `.tile` rules, copied verbatim from `src/app/web/static/style.css:72-75`:

```css
  .visually-hidden {
    position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
  }
```

Then check the existing `.tile[aria-pressed="true"]` / `[aria-pressed="false"]` rules (around lines 791-810). They no longer match anything. Replace their selectors with the checkbox equivalents so the tiles still show selected/unselected states:

```css
  .tile:has(input:checked) { /* keep the existing selected declarations */ }
  .tile:has(input:not(:checked)) { /* keep the existing unselected declarations */ }
  .tile:has(input:not(:checked)):hover { opacity: .62; }
  .tile:has(input:checked) .tick { /* keep the existing tick declarations */ }
```

Copy the declaration bodies verbatim from the rules you are replacing — do not retype them from memory.

- [ ] **Step 4: Open the demo and confirm both surfaces still look right**

Open `docs/superpowers/demo/dekimasen-demo.html` in a browser. Confirm: the tag dialog shows nine removable member chips, and the setup tiles still render with their ticks and their selected/unselected contrast in **both** light and dark (set `data-theme` in devtools — see the note at the top of Branch B).

The `:has()` selectors in Step 3 are the risk here: if the tiles lose their selected/unselected contrast entirely, that is this task failing, not a cosmetic nit. Report it rather than working around it.

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/demo/dekimasen-demo.html
git commit -m "$(cat <<'EOF'
docs(demo): the two places the demo was wrong, not the app

The member list is an EDIT surface -- every chip has a delete x -- so the
"+N more" truncation made five of nine members unreachable on the one
page built for reaching them.

The pick tiles render identically either way, but a label wrapping a
checkbox submits with JS off and <button aria-pressed> cannot.

Both carry a comment saying why, so neither gets "fixed" back.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

### Task 5: Four error-page frames

**Files:**
- Modify: `docs/superpowers/demo/dekimasen-demo.html` (new section)

**Interfaces:**
- Consumes: nothing.
- Produces: nothing.

**Why:** A genuinely new surface with no frame to amend, so whoever next reworks error copy or chrome has nothing to work against and will invent one.

The app renders **one template with four sets of copy** (`src/app/web/templates/error.html`, copy chosen in `src/app/web/app.py:244-267`). Copy verbatim, do not paraphrase:

| Code | Heading | Body |
|---|---|---|
| 404 | Page not found | That page doesn't exist. The link may be out of date. |
| 403 | No access | You're signed in as **reiji**, which doesn't have access to that page. |
| 422 | That didn't go through | Nothing was saved. Here's what needs fixing: |
| 500 | Something broke | Something broke on our end, and it's been logged. Nothing you did caused it. |

403 has a second variant when no username is known — "That page needs an account with more access than yours." Show the named variant; mention the other in a comment.

- [ ] **Step 1: Add the section**

Place it after the existing page frames, following the file's section conventions. Four frames:

```html
<!-- ============ error pages ============================================
     One template (web/templates/error.html), four sets of copy. Each code
     says the one useful thing it can: a 404 has nothing to explain, a 422 is
     useless without its messages, and a 403 is almost always the wrong
     Discord account -- which nothing else on the page would tell you.
     Only ever rendered for a full-page NAVIGATION; an XHR keeps the JSON body
     it was already parsing, because the quick-create dialogs read .detail off
     a 409 to offer select-existing.
     The heading is .lede h1. ==================================== -->

<div class="lede">
  <h1>Page not found</h1>
  <p class="dim">That page doesn't exist. The link may be out of date.</p>
</div>
<p><a class="btn" href="#">Back to home</a></p>

<div class="lede">
  <h1>No access</h1>
  <!-- Naming the account is the whole point. When no username is known the
       body reads "That page needs an account with more access than yours." -->
  <p class="dim">You're signed in as reiji, which doesn't have access to that page.</p>
</div>
<p><a class="btn" href="#">Back to home</a></p>

<div class="lede">
  <h1>That didn't go through</h1>
  <p class="dim">Nothing was saved. Here's what needs fixing:</p>
</div>
<!-- 422 only. Without these the page is strictly worse than the JSON blob it
     replaces -- the blob at least said what was wrong. -->
<div class="banner warn">
  <ul>
    <li>Event id "new" is reserved</li>
    <li>Closes at must be a real date and time</li>
  </ul>
</div>
<!-- A real button, not a link: a 422 is almost always a form POST, browsers
     restore form values on a history back, and there is nothing to bookmark. -->
<p><button type="button" class="btn">Go back and fix it</button> <a class="btn" href="#">Back to home</a></p>

<div class="lede">
  <h1>Something broke</h1>
  <p class="dim">Something broke on our end, and it's been logged. Nothing you did caused it.</p>
</div>
<p><a class="btn" href="#">Back to home</a></p>
```

Wrap each frame in whatever card/section element the surrounding demo uses for a page frame, and give each a caption naming its code — read the neighbouring sections and match them.

- [ ] **Step 2: Check `.lede` and `.banner warn` exist in the demo's CSS**

Search the `<style>` block. If `.lede` is missing, add it from the app, including the balanced wrap Task 3 adds:

```css
  .lede { max-width: 58ch; }
  .lede h1 { margin: 0 0 .4rem; font-size: 1.6rem; letter-spacing: -.02em; font-weight: 700; text-wrap: balance; }
  .lede p { margin: 0; color: var(--dim); }
```

- [ ] **Step 3: Open the demo and confirm all four frames render in both themes**

- [ ] **Step 4: Commit**

```bash
git add docs/superpowers/demo/dekimasen-demo.html
git commit -m "$(cat <<'EOF'
docs(demo): frames for the four error pages

They shipped 2026-07-28 as a genuinely new surface -- a full page with its
own copy per status code -- and no demo had a frame for any of them, so
whoever next reworks that copy had nothing to work against.

One template, four sets of copy, verbatim from web/app.py.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

### Task 6: Split pill and subunit rail

**Files:**
- Modify: `docs/superpowers/demo/dekimasen-demo.html` (concert page performers block; Tags page group rows)

**Interfaces:**
- Consumes: nothing.
- Produces: nothing.

**Why:** Both shapes shipped 2026-08-01 with no demo frame. **The design is already decided** — this is a port, not a decision. From `docs/superpowers/specs/2026-08-01-character-seiyuu-subunit-design.md`:

> **Merged chip shape: the split pill** (owner, from four mockups). One chip visibly made of two halves, each its own link. Chosen over the inline `如月千早（今井麻美）` form specifically because the merge is conditional: when only one end is present the chip is plain, and the split shape makes that difference read as meaningful rather than as inconsistent styling.

> **Subunit shape: indented rail, repetition kept** (owner, from four mockups). The alternative — the subunit absorbing its members so no name appears twice — was rejected: it makes the parent cluster stop being a truthful lineup.

- [ ] **Step 1: Add the CSS**

Port these from `src/app/web/static/style.css:1101-1118` into the demo's `<style>` block, keeping the explanatory comment:

```css
  /* A character and her seiyuu, rendered as ONE chip in two halves, each its
     own link. Only when BOTH ends are attached; either alone falls to a plain
     chip, and the split shape is what makes that difference read as meaningful
     rather than as inconsistent styling.
     The box is DERIVED from .performers .chip's: same vertical padding, same
     explicit line-height (a chip SETS 1.5 -- the body's 1.6 is what a half gets
     if that line goes), same 1px border. */
  .mchip { display: inline-flex; align-items: stretch; border-radius: 999px;
           overflow: hidden; background: var(--chip); font-size: .82rem;
           border: 1px solid transparent; }
  .mchip > * { padding: .22rem .55rem; color: var(--ink); text-decoration: none;
               line-height: 1.5; }
  .mchip > .cv { color: var(--dim); border-left: 1px solid var(--line); }
  .performers .mchip .nolink { opacity: .75; }

  /* A subunit sits beneath its parent. Indent + rail, NOT a nested card: the
     two-shape callout grammar has no third shape and this is not a callout. */
  .pcluster.sub { margin-left: 1rem; padding-left: .85rem; border-left: 2px solid var(--line); }
  .grow2.sub { margin-left: calc(var(--d, 1) * 1rem); border-left: 2px solid var(--line); }
```

- [ ] **Step 2: Add the concert-page frame**

In the demo's concert page performers block, add a cluster showing a split pill beside a plain chip (so the conditional merge is visible), and a nested subunit cluster:

```html
      <div class="pcluster">
        <div class="pclabel"><span class="chip grp">765PRO ALLSTARS</span></div>
        <div class="chiprow">
          <!-- BOTH ends attached: one chip, two halves, each its own link. -->
          <span class="mchip"><a class="cn" href="#">如月千早</a><a class="cv" href="#">今井麻美</a></span>
          <span class="mchip"><a class="cn" href="#">天海春香</a><a class="cv" href="#">中村繪里子</a></span>
          <!-- Only ONE end attached: a plain chip, indistinguishable from any
               other performer chip. That contrast is the whole design. -->
          <a class="chip" href="#">諏訪彩花</a>
        </div>
      </div>
      <!-- depth is 1 only when the parent group is attached too, so the rail
           marks a relationship the reader can see both ends of. A subunit whose
           parent is absent renders as an ordinary cluster -- no indent, no
           label (owner rule). -->
      <div class="pcluster sub">
        <div class="pclabel"><span class="chip grp">竜宮小町</span> <span class="tiny dim">Subunit</span></div>
        <div class="chiprow">
          <span class="mchip"><a class="cn" href="#">秋月律子</a><a class="cv" href="#">若林直美</a></span>
          <a class="chip" href="#">三浦あずさ</a>
        </div>
      </div>
```

- [ ] **Step 3: Add the Tags-page subunit rail**

In the Tags page section, add a `.grow2.sub` row beneath an ordinary `.grow2` so the indent reads as a relationship. Match the surrounding rows' markup exactly — read them first.

- [ ] **Step 4: Open the demo and confirm in both themes**

Confirm the pill's two halves share one border and one background, the divider between them is visible, and the plain chip beside them is clearly a different shape.

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/demo/dekimasen-demo.html
git commit -m "$(cat <<'EOF'
docs(demo): the split pill and the subunit rail

Both shipped 2026-08-01 with no demo frame. A port, not a decision -- the
shape and its rejected alternative are recorded in that day's spec, which
the frames quote.

The concert-page frame deliberately shows a split pill NEXT TO a plain
chip: the merge is conditional, and that contrast is the whole reason the
split shape won.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

### Task 7: Goods-sale rounds — the select and the two lines

**Files:**
- Modify: `docs/superpowers/demo/dekimasen-demo.html` (editor round card; concert page round rows)

**Interfaces:**
- Consumes: nothing.
- Produces: nothing.

**Why:** Shipped 2026-08-02 with no frame. The select is the interesting one: it is the first control on a card that **hides itself** when no item-sale round exists, which a static frame has to decide how to depict.

- [ ] **Step 1: Add the select row to the demo's editor round card**

Source is `src/app/web/templates/_editor_round_card.html:86-98`:

```html
        <!-- Hidden (never removed -- a hidden div still submits its fields)
             while no item-kind round exists on the concert; a script keeps its
             options and visibility current as rows change. Shown here in its
             PRESENT state; with no item-sale round on the concert this whole
             row is absent from the card. -->
        <div class="redit-requires">
          <label class="dim tiny">Requires item from
            <select>
              <option value="">No item needed</option>
              <option selected>Goods pre-order (closes 20 Sep)</option>
            </select>
          </label>
        </div>
```

- [ ] **Step 2: Add the two concert-page lines**

Source is `src/app/web/templates/_round_rows.html:102-103`:

```html
          <span class="more">🛍️ Requires: Goods pre-order — sale ends Sat 20 Sep · 23:59 JST, 10:59 ADT</span>
          <span class="more">Needed for: FC presale, General sale</span>
```

Place them on an existing round row in the demo's concert page, inside whatever element holds that row's other `.more` lines.

- [ ] **Step 3: Check `.redit-requires` exists in the demo CSS**

If absent, port it from `style.css`. Search the app stylesheet for `.redit-requires` and copy the rule verbatim; if the app has no rule for it (it may rely on `.dim`/`.tiny` alone), add nothing.

- [ ] **Step 4: Open the demo and confirm both surfaces**

- [ ] **Step 5: Commit**

```bash
git add docs/superpowers/demo/dekimasen-demo.html
git commit -m "$(cat <<'EOF'
docs(demo): the goods-sale select, and what a round requires

Shipped 2026-08-02 with no frame. The select is the first control on a
card that hides ITSELF when no item-sale round exists, so the frame shows
it present and says in a comment what its absent state means.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

### Task 8: Import preview — evidence, rejection, paste fold

**Files:**
- Modify: `docs/superpowers/demo/dekimasen-onboarding-demo.html`

**Interfaces:**
- Consumes: nothing.
- Produces: nothing.

**Why:** Shipped 2026-08-06 with no frame. This one is better-behaved than the others: all three compose the existing two-shape callout grammar rather than inventing a shape, so what is owed is a frame showing them in place. **This is the only Branch B task in the onboarding demo**, which already owns import preview.

- [ ] **Step 1: Add the per-round evidence block**

Source is `src/app/web/templates/_editor_round_card.html:115-122`. Place it inside a round card in the demo's import preview:

```html
        <!-- What an AI completion pass quoted for this round's timestamps, when
             one ran. Optional and absent everywhere else: the create and edit
             pages pass nothing, so their cards render exactly as they did
             before the feature existed. .edgecard because it is ongoing state
             rather than something needing attention (the callout grammar). -->
        <div class="edgecard ok evidence-quote">
          <p class="dim tiny">Read from the ticket page:</p>
          <p class="tiny"><b>apply_opens_jst</b> — 受付期間：2025年9月20日(土)12:00〜</p>
          <p class="tiny"><b>apply_closes_jst</b> — 2025年9月28日(日)23:59まで</p>
        </div>
```

- [ ] **Step 2: Add the rejection callout above the rounds section**

Source is `src/app/web/templates/import_preview.html:115-128`:

```html
      <!-- What the completion pass proposed but could not ground on the page it
           read. A real deadline the checker could not quote is reported rather
           than silently dropped: a quiet loss is exactly as harmful as a quiet
           invention. .banner because this needs attention. -->
      <div class="banner warn">
        <p>The AI proposed these and I could not find them on the page — check them by
          hand before trusting them:</p>
        <ul>
          <li class="tiny">2次抽選 closes — no quote found for "2025年10月5日"</li>
        </ul>
        <p class="tiny">Source: <a href="#" rel="nofollow noopener">eplus.jp/aqours-5th</a></p>
      </div>
```

- [ ] **Step 3: Add the paste-a-page fold**

Source is `src/app/web/templates/import_preview.html:263-273`:

```html
      <details class="fold">
        <summary>Fill rounds from a page I paste</summary>
        <div class="fold-body">
          <p class="dim tiny">Open the official ticket page, select all, and paste it here. Only
            deadlines quoted from what you paste will be kept.</p>
          <textarea rows="6" style="width:100%"></textarea>
          <button class="btn" type="button">Read this page</button>
        </div>
      </details>
```

- [ ] **Step 4: Check `.edgecard`, `.banner warn` and `.fold` exist in the onboarding demo's CSS**

Port any that are missing from `src/app/web/static/style.css`, verbatim.

- [ ] **Step 5: Open the onboarding demo and confirm all three in both themes**

- [ ] **Step 6: Commit**

```bash
git add docs/superpowers/demo/dekimasen-onboarding-demo.html
git commit -m "$(cat <<'EOF'
docs(demo): the import preview's evidence, rejection and paste fold

Shipped 2026-08-06 with no frame. Better-behaved than the other gaps:
all three compose the existing two-shape callout grammar rather than
inventing a shape, so what was owed is a frame showing them in place.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

### Task 9: Bookkeeping

**Files:**
- Modify: `docs/architecture.md:1140-1142`
- Modify: `WISHLIST.md`

**Interfaces:**
- Consumes: every task above must be complete and committed.
- Produces: nothing.

- [ ] **Step 1: Delete the known-gap note in architecture.md**

Lines 1140-1142 currently read:

```
  Known gap: the 403/404/422/500 error pages shipped with no demo frame, and
  the signed-out `.signin-note` never got one either. Both are logged in
  WISHLIST's demo-parity cosmetics entry rather than left to be rediscovered.
```

This design closes both — the error pages by Task 5, and `.signin-note` because it was never really a gap (the class was absorbed into `.banner` by the 2026-07-24 UX pass, and `dekimasen-ux-pass-demo.html` already carries it). **Delete the note entirely**; do not rewrite it.

- [ ] **Step 2: Move entry #8 to Shipped**

Follow `CLAUDE.md`'s wishlist rule. The Shipped entry must record, because none of it is recoverable from the diff:

- The owner replaced "the demo is right" with "judge each on merit", and **two of the items reversed direction** — the Tags member list and the Setup pick tiles now change the demo, not the app. Both would have been regressions.
- **Item 2 was struck, not built**: it asked for a frame for `.signin-note`, a class deleted three days after the item was filed.
- **The split pill's "open design question" was not open** — the choice and its reasoning are in the 2026-08-01 spec.
- The Following cosmetic left earlier the same day and became its own unranked entry.
- The `text-wrap` claim in the spec was **corrected during planning**: only one of the three selectors carrying `text-wrap: balance` is an `h1`, so the test pins a set rather than sweeping. Record what the owner decided about `.legal h1`.

- [ ] **Step 3: Full revision pass over the remaining entries**

Per `CLAUDE.md`: re-rank by impact, reconsider which are still useful, and write the revision-pass narrative note. Note that this build removed nothing from anyone else's path — it touched demos, one label, one dialog footer and one CSS declaration.

- [ ] **Step 4: Run the full suite and lint one last time**

Run: `uv run --isolated pytest -q` then `uv run --isolated ruff check .`

- [ ] **Step 5: Commit**

```bash
git add docs/architecture.md WISHLIST.md
git commit -m "$(cat <<'EOF'
docs: entry #8 ships, and the known-gap note goes with it

The merit review reversed two of its items and struck a third as
work that stopped existing three days after it was filed.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01NMLgHrqvFFmFatWBeJuCGH
EOF
)"
```

---

## Self-review notes

**Spec coverage.** Every section of the spec maps to a task: A1→Task 1, A2→Task 2, A3→Task 3, B1+B2→Task 4, B3→Task 5, B4→Task 6, B5→Task 7, B6→Task 8, "What gets recorded"→Task 9. The spec's "Out of scope" items appear in no task, correctly.

**No blocked tasks.** The one open question (`.legal h1`) was ruled on 2026-08-11: fix both, so the pinned set expresses a coherent rule rather than an arbitrary list.

**Sequencing.** Tasks 4-7 share one file and must run one at a time. Task 9 runs last. Tasks 1, 2, 3 and 8 touch files nothing else touches.

**Test honesty.** Tasks 1, 2 and 3 each include an explicit step that verifies the test fails against its named mutation, because a test that cannot fail is the failure mode this project has paid for repeatedly.
